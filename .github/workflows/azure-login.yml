trigger: none

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureCLI@2
  displayName: "Test Azure Service Principal Login"
  inputs:
    azureSubscription: "YOUR-SERVICE-CONNECTION-NAME"
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      echo "Login successful!"
      az account show --output table